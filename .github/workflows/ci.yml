name: ICEDTEA CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
        
    - name: Huh?
      run: |
        echo "NICE_TRY_XDadsafasfd"
